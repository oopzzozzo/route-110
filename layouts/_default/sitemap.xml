{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{- $lang := .Lang -}}
{{- $taxKinds := slice "taxonomy" "term" -}}
{{- $taxPages := where (where site.AllPages "Kind" "in" $taxKinds) "Lang" $lang -}}
{{ range .Data.Pages }}{{ if not (in $taxKinds .Kind) }}{{ if and (.Param "xml" | default true) (or (not .Params.externalUrl) (and (.Params.externalUrl) (and (not (hasPrefix .Params.externalUrl "http://")) (not (hasPrefix .Params.externalUrl "https://"))))) }}
{{- if .Permalink -}}{{ $link := .Permalink }}{{ if .Params.externalUrl }}{{ $link = printf "%s%s" .Site.BaseURL (substr .Params.externalUrl 1) }}{{ end }}
  <url>
    <loc>{{ $link }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}{{ if .IsTranslated }}{{ range .Translations }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.LanguageCode }}"
      href="{{ .Permalink }}"
    />{{ end }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.LanguageCode }}"
      href="{{ $link }}"
    />{{ $defaultPage := index (sort .AllTranslations "Language.Weight") 0 }}{{ with $defaultPage }}
    <xhtml:link
      rel="alternate"
      hreflang="x-default"
      href="{{ .Permalink }}"
    />{{ end }}{{ end }}
  </url>
{{- end -}}{{ end }}{{ end }}{{ end }}
{{ range $taxPages }}
{{- if .Permalink -}}{{ $link := .Permalink }}
  <url>
    <loc>{{ $link }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}{{ if .IsTranslated }}{{ range .Translations }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.LanguageCode }}"
      href="{{ .Permalink }}"
    />{{ end }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.LanguageCode }}"
      href="{{ $link }}"
    />{{ $defaultPage := index (sort .AllTranslations "Language.Weight") 0 }}{{ with $defaultPage }}
    <xhtml:link
      rel="alternate"
      hreflang="x-default"
      href="{{ .Permalink }}"
    />{{ end }}{{ end }}
  </url>
{{- end -}}{{ end }}
</urlset>
